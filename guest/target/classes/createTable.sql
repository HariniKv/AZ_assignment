CREATE TABLE GUEST(
   GUESTID   INT              NOT NULL,
   GUESTNAME VARCHAR (20)     NOT NULL,
   HOTEL VARCHAR (20)     NOT NULL,
   RESERVATIONNUMBER  INT (25) ,       
   PRIMARY KEY (GUESTID)
);